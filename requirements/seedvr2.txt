# Optional SeedVR2 upscaling runtime (third_party/seedvr2 submodule).
# Also run: pip install -e third_party/seedvr2
-r base.txt
diffusers>=0.33.1           # SeedVR2 needs a higher floor than core
safetensors
psutil
einops
omegaconf>=2.3.0
peft>=0.17.0
rotary_embedding_torch>=0.5.3
gguf
