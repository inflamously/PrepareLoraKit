## Description
- This package describes the pipeline path
- Configuration determines which step is dependent
- Also it allows to read the order
- Include are also the configs that are loaded each step containing parameters