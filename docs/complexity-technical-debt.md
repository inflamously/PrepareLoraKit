# Complexity Technical Debt

Audited on 2026-06-29. This list highlights files that exceed roughly 300 lines of code, carry too many responsibilities in one place, or are otherwise harder than necessary to read and modify. Checked-in third-party code is called out separately from first-party refactor candidates.

| Filepath | Description                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- |---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `third_party/seedvr2/` | TO_BE_IGNORED: 25,814 LoC of checked-in third-party integration; large files cover inference CLI, model configuration/loading, generation phases, memory management, optimization, VAE/DiT internals, and image/video upscaling, so local changes should be tracked separately from first-party cleanup.                                                                                                      |
| `prepare_lora_kit/steps/caption_bbox/vlm.py` | 601 LoC; owns Hugging Face model loading, quantization/dtype resolution, CUDA/VRAM probing, image downscale/preprocessing, prompted and image-to-text generation adapters, classic caption composition, module-level cache lifecycle, the `CaptionRuntime` class with status payloads, and the public `caption_image`/`caption_region`/`unload` helpers.                                                      |
| `tests/ui/test_runner.py` | 580 LoC; broad UI runner suite covering selection/prerequisite validation, project payloads, substep resolution, run sequencing and force-run, step-config pause/override, static media serving and shutdown settings, VAE/curate interaction payloads, log-stream unicode/ANSI handling, rich console capture, cancellation, job snapshots, and region caption cropping. Would be clearer split by behavior. |
| `prepare_lora_kit/steps/upscale/step.py` | 415 LoC; one module handles output-context setup, image partitioning, upscale-model normalization and deprecation warnings, SeedVR2 resolution/fallback, batch and single-image processing, hallucination rejection, accept/passthrough logic, temp cleanup, exception formatting, and report writing.                                                                                                        |
| `tests/project/test_config.py` | 382 LoC; covers project YAML parsing, default project generation, legacy migrations, substep flag parsing, step ordering, optional-upscale behavior, SeedVR2 catalog/config/residency validation, and UI payload metadata in one suite.                                                                                                                                                                       |
| `prepare_lora_kit/ui/runner/manager.py` | 358 LoC (up from 304); `JobManager` owns job lifecycle and single-active-job locking, cancellation, project-status reporting, project/network loading, step-config resolution, substep selection and prerequisite validation, and a ~120-line `_execute` orchestration method covering pipeline run-through, interaction dispatch, and result payloads.                                                       |
| `prepare_lora_kit/ui/nflamously.css` | 343 LoC; self-contained design system bundling fonts, color/typography/spacing tokens, base reset, and glass/eyebrow utilities. **Appears unreferenced** — no HTML/JS/Python imports it — so verify it is dead before splitting; if live, it overlaps `foundation.css` tokens.                                                                                                                                |
| `prepare_lora_kit_ui/static/core/foundation.css` | 310 LoC; combines `:root` tokens, global element resets, typography defaults, form/button primitives, scrollbar styling, and panel/modal/component base behavior — token layer and component layer would be clearer apart.                                                                                                                                                                                    |
| `prepare_lora_kit/project/registry.py` | 284 LoC; below threshold but high intent density: default pipeline/substep seed data, project CRUD (create/write/update-meta/delete/duplicate), input-dir mutation, config-path resolution, and load-or-create normalization layered over the shared `ConfigRegistry`. Mixes default-data templates, persistence, and migration helpers.                                                                      |
| `prepare_lora_kit/steps/upscale/seedvr2_worker.py` | 275 LoC; below threshold but high integration density: subprocess CLI request/response IO, dynamic SeedVR2 import, model download, per-item processing, full argument construction, device selection, model-residency heuristics, and error normalization.                                                                                                                                                    |
